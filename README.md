
# java_compile Step
Install dependencies use `mvn compile`

### INPUTS

## EXAMPLE 

```yml
steps:
  - name: java_build
    enable: true
    failure: true
    plugin:
      name: java_build
      inputs:
```
