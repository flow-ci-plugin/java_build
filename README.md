
# mvn_compile Step
Install dependencies use `mvn compile`

### INPUTS

## EXAMPLE 

```yml
steps:
  - name: mvn_build
    enable: true
    failure: true
    plugin:
      name: mvn_build
      inputs:
```
